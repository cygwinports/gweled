Fixes crash/freeze when exiting.

Patch from Gentoo ebuild's sed commands

--- origsrc/gweled-0.7/src/graphic_engine.c	2005-05-25 16:06:30.000000000 -0500
+++ src/gweled-0.7/src/graphic_engine.c	2008-12-21 21:58:44.834250000 -0600
@@ -213,7 +213,7 @@
 		gweled_draw_character (msg_x + i * FONT_WIDTH, msg_y, 1,
 				       message[i]);
 
-	free (message);
+	g_free (message);
 }
 void
 gweled_draw_message (gchar * in_message)
@@ -250,7 +250,7 @@
 			sge_object_set_lifetime (p_object,
 						 (int) (50.0 * timing));
 		}
-	free (message);
+	g_free (message);
 }
 
 int
--- origsrc/gweled-0.7/src/main.c	2005-05-25 16:44:04.000000000 -0500
+++ src/gweled-0.7/src/main.c	2008-12-21 21:57:34.990500000 -0600
@@ -206,7 +206,6 @@
  					else
  						buffer = g_strdup_printf ("%.0f", scores[i]);
  					gtk_label_set_markup (GTK_LABEL (label), buffer);
- 					g_free (buffer);
  				}
 				else
  					gtk_label_set_text (GTK_LABEL (label), "");
@@ -365,7 +364,7 @@
 	if(swap_sfx)
 		Sample_Free(swap_sfx);
 	if(click_sfx)
-		Sample_Free(swap_sfx);
+		Sample_Free(click_sfx);
 
 	MikMod_Exit();
 
